export enum PlaylistError {
  Name = "Must be 3 - 20 characters. Must not have digits, and symbols.",
  Description = "Must be 100 characters max.",
  Access = "Must be Public, Private or Protected.",
}
